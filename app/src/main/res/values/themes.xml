<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- The base Café Theme using Material Components -->
    <style name="Base.Theme.CafeTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Primary brand colors -->
        <item name="colorPrimary">@color/coffee_brown</item>
        <item name="colorPrimaryDark">@color/coffee_brown</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary / accent colors -->
        <item name="colorSecondary">@color/latte_brown</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Background -->
        <item name="android:windowBackground">@color/cream_background</item>
        <item name="android:colorBackground">@color/cream_background</item>

        <!-- Toolbar -->
        <item name="android:toolbarStyle">@style/Widget.MaterialComponents.Toolbar.Primary</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/coffee_brown</item>
    </style>

    <!-- cafe theme -->
    <style name="Theme.CafeTheme" parent="Base.Theme.CafeTheme" />

</resources>
